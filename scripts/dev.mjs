#!/usr/bin/env node
// Ce fichier a été remplacé volontairement. Utilisez `npm run dev` qui exécute `next dev`.
// Conserver un placeholder pour éviter les erreurs si le fichier est invoqué directement.
console.error("scripts/dev.mjs supprimé — utilisez 'npm run dev' (exécute 'next dev')");
process.exit(1);
